import { BuscarTipoPipe } from './buscar-tipo.pipe';

describe('BuscarTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
