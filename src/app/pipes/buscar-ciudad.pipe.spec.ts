import { BuscarCiudadPipe } from './buscar-ciudad.pipe';

describe('BuscarCiudadPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarCiudadPipe();
    expect(pipe).toBeTruthy();
  });
});
